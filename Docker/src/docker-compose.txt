version - версия

volumes - имена volumes

services - имена сервисов

параметры образа:
    image - имя образа

    build - имя директории из которой будет билдится образ

    restart -

    envirement - переменные окружения образа

    ports - проброс портов