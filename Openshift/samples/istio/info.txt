Gateway (возможность указывать порты, протокол и хосты, для которых мы хотим разрешить входящий трафик) ->

VirtualService (указывает Ingress Gateway'ю, как маршрутизировать запросы, которые разрешены внутри кластера) ->

DestinationRule (дополнительная маршрутизация, если в VirtualService есть subset)

ServiceEntry (запись в реестр istio,  чтобы эти сервисы можно было рассматривать как любой другой сервис в сетке. для внешних?)