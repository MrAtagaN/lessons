git config <параметр> <значение> - Установить параметры git
    Пример:
    git config --global user.name <USER_NAME>

    --list - Просмотр всех параметров git
    --global - Параметры для пользователя в системе
    --system - Общие параметры системы
    --local - По умолчанию, параметры в директории .git

git push - Запушить в репозиторий с именем и паролем:
    git push https://username:password@github.com/username/repository.git

git init - создать локальный репозиторий

git add - добавить файл под версионный контроль

git rm <имя файла> - Удадяет файл
    --cached - Оставляет файл не под версионным контролем

git status
