package collections;


import java.util.Iterator;
import java.util.Spliterator;

/**
 * {@link Iterable}
 *
 * forEach - Выполняет заданное действие для каждого элемента Iterable до тех пор, пока все элементы не будут обработаны или действие не вызовет исключение.
 * iterator - возвращает {@link Iterator}
 * spliterator - возвращает {@link Spliterator}
 *
 */
public class Iterable_ {

    public static void main(String[] args) {

    }
}
